---
const { value, description }  = Astro.props
---

<article class="flex flex-col items-start justify-center gap-6 bg-gray-200 bg-opacity-40 p-8 rounded-2xl max-w-[420px]">
          
    <div class="flex flex-col">
      <div class="w-[2rem]">
        <slot name="icon"/>
      </div>
    </div>
    <h3 class="font-semibold text-xl">{value}</h2>

    <p class="text-[15px] text-justify">{description}</p>
  </article>